{
  // android/ is generated by `expo prebuild` (Continuous Native Generation). Letting the Java/Gradle
  // extensions import it keeps it locked on Windows, which breaks prebuild; builds use the CLI.
  "gradle.autoDetect": "off",
  "java.import.gradle.enabled": false
}
